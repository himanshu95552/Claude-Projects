# Fonts

Latin subsets of **Inter** and **Space Grotesk** (variable, woff2), from Google Fonts.
Both are licensed under the SIL Open Font License 1.1 (https://openfontlicense.org),
which permits bundling. `render_carousel.cjs` embeds them so carousels render in the
brand fonts even without network access.
